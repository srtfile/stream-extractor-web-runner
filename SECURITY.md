# Security notes

- Use this only for media URLs you own or are authorized to process.
- Do not hard-code GitHub tokens in `docs/index.html`.
- Prefer a private repository if submitted URLs or results should be private.
- Public GitHub Pages output under `results/` can be viewed by anyone who knows the URL.
- GitHub Actions logs and artifacts may contain submitted URLs and extraction errors.
